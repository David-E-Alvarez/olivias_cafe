import { Link } from "gatsby"
import React from "react"
import "../styles/global.css"

const Header = () => (
  <header>
    <nav>
      <h1>Olivias Cafe</h1>
    </nav>
  </header>
)

export default Header
